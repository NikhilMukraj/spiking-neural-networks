# c2 weight of 0/c2 bayesian cue off
# memory_biases_memory = false
# iterations1 = 5000
# iterations2 = 5000
# pattern_switch = true
# trials = 30
# change gmax of ampa to see if pattern_switch accuracy changes at all
